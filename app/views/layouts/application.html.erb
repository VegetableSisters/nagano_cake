<!DOCTYPE html>
<html>
  <head>
    <title>NaganoCake</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/28e899c482.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    
    <header>
    <nav>
    <div class="container">

    <!--リンクとリンクの間隔を広げたい-->
    <!--顧客< if customer_signed_in? %><end%>-->
    <!-- さらに管理者< elsif admin_signed_in? %>で場合分け、管理者でログインしてないなら<else%>で返す<end%>-->
    
    
    <ul class="nav justify-content-end">
    <% if customer_signed_in? %>
    <li class="nav-item">
      <%= link_to "マイページ", customers_my_page_path, class: "btn btn-outline-dark" %></li>
    <li class="nav-item">
      <%= link_to "商品一覧", items_path, class: "btn btn-outline-dark" %></li>
     <li class="nav-item">
      <%= link_to "カート", cart_items_path, class: "btn btn-outline-dark" %></li>
    <li class="nav-item">
      <%= link_to "ログアウト", destroy_customer_session_path, method: :delete, class: "btn btn-outline-dark" %></li>
    </ul>
    
    <% elsif admin_signed_in? %>
      <%= link_to "ログアウト", destroy_customer_session_path, method: :delete, class: "btn btn-outline-dark" %></li>
    
    <!--非会員-->
    <%else%>
      <li class="nav-item">
      <%= link_to "About", root_path, class: "btn btn-outline-dark" %></li>
    <li class="nav-item">
      <%= link_to "商品一覧", items_path, class: "btn btn-outline-dark" %></li>
     <li class="nav-item">
      <%= link_to "カート", cart_items_path, class: "btn btn-outline-dark" %></li>
    <li class="nav-item">
      <%= link_to "ログイン", new_customer_session_path, class: "btn btn-outline-dark" %></li>
    </ul>
    <%end%>
    
    <!--検索機能-->
    <%= form_with url:  items_path, method: :get, local: true do |f| %>
    <%= f.text_field :q, placeholder: "Search", value: "" %>
    <%= f.submit "検索", name: nil %>
    <% end %>
  
    
    </div>
    </nav>
    </header>
    
      <%= yield %>
   
  </body>
  
  
  
  
  
</html>
